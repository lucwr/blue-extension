# Extension build-time variables.
# Copy to .env to override. Anything prefixed with VITE_ is exposed to the bundle.

# Used by the popup if the user hasn't customized the backend URL in Settings yet.
# IMPORTANT: never store secrets here — the extension bundle is publicly readable.
VITE_DEFAULT_BACKEND_URL=http://localhost:8787
